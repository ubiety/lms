- breadcrumb :new_user
.row
  .col
    = form_for @user do |f|
      .card
        .card-header
          %h3= _('Create User')
        .card-body
          = render 'users/form', f: f
        .card-footer
          = f.submit class: 'btn btn-primary float-right'
