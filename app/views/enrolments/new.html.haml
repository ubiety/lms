= form_for course_enrolment_path(@course, @enrolment) do |form|
  .card
    .card-header
      %h3
        = _('Enroll Student in')
        = @course.name
    .card-body
      = render 'enrolments/form', form: form, students: @students, enrolment: @enrolment
    .card-footer
      = f.submit _('Enroll Student(s)'), class: 'btn btn-primary float-right'
