= form_for @course do |form|
  .card
    .card-header
      %h4= _('Edit Course')
    .card-body
      = render 'courses/form', form: form, instructors: @instructors, course: @course
    .card-footer
      = form.submit class: 'btn btn-primary float-right'
